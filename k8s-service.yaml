apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  namespace: default
  labels:
    app: ecommerce-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: ecommerce-app
